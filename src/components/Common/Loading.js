import React from "react";
import loading from "./../../assets/img/loading.gif";


const Loading = () => (
      <img className="photohome" alt={loading} src={loading} />  
);

export default Loading;
